#!/bin/bash

# Automatically activate virtual environment for farm_manager
if [ -d "venv_farm_manager" ]; then
    source venv_farm_manager/bin/activate
fi

# Optional: Set any project-specific environment variables
export FARM_MANAGER_ENV=development
